class Service {
    constructor(host, key, version, port) {
        this.host = host;
        this.key = key;
        this.version = version;
        this.port = port;
    }
};

module.exports = Service;